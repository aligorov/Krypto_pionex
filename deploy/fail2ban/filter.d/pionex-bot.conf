# Fail2ban filter for Pionex Control Bot
# Matches structured stdout/journal logs emitted on authentication failures.

[Definition]
failregex = ^.*\[AUTH_FAILURE\] ip="<HOST>".*$
            ^.*\[AUTH_FAILURE\] ip="<HOST>" user=.*$

ignoreregex =
